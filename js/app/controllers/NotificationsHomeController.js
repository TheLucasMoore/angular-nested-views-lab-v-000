function NotificationsHomeController() {
  // not needed here, but is the structure needed eventually
}

angular
  .module('app')
  .controller('notificationsHomeController', NotificationsHomeController)